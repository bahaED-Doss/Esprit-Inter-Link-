import 'package:flutter/material.dart';
class StudentTestPage extends StatelessWidget {
  const StudentTestPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Test étudiant')), body: const Center(child: Text('Test étudiant')));
  }
}

