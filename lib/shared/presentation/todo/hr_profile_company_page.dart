import 'package:flutter/material.dart';
class HRProfileCompanyPage extends StatelessWidget {
  const HRProfileCompanyPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Profil & Société')), body: const Center(child: Text('Profil & Société RH')));
  }
}

