import 'package:flutter/material.dart';
class PMInternsPage extends StatelessWidget {
  const PMInternsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Stagiaires')), body: const Center(child: Text('Stagiaires PM')));
  }
}

