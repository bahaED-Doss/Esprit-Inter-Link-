import 'package:flutter/material.dart';
class HRApplicationsPage extends StatelessWidget {
  const HRApplicationsPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Candidatures')), body: const Center(child: Text('Candidatures RH')));
  }
}

