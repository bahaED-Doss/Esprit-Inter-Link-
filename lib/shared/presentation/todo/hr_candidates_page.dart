import 'package:flutter/material.dart';
class HRCandidatesPage extends StatelessWidget {
  const HRCandidatesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Candidats')), body: const Center(child: Text('Candidats RH')));
  }
}

